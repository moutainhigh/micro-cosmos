package com.tairan.activiti.demo.dynamic;

/**
 * @Author:thomas
 * @Date: 2018/6/27 20:31
 * @Description:
 */
public class RelatedKnowledger {
    public void test180627_001(){

//对应activiti:ruleVariablesInput
      /*  if (variablesInputExpressions != null) {
            Iterator<ConditionAnalyzer.Expression> itVariable =   variablesInputExpressions.iterator();
            while (itVariable.hasNext()) {
                ConditionAnalyzer.Expression variable = itVariable.next();
                //pojo插入到workMemory，成为fact对象，返回一个pojo的引用
                ksession.insert(variable.getValue(execution));
            }
        }


        //根据过滤或留下（根据配置否exclude）规则文   //件。。。略
        filter.setAccept(!exclude);
        //触发规则
        ksession.fireAllRules(filter);


        规则的输出将作为流程变量被保存在activiti:resultVariable所定义的值中*/
    }
}
